/cache/

# Python virtual environment created by scripts/build-race-library.ps1
# (or manually per README) -- local execution environment only, never
# committed. Recreate with: python -m venv .venv
/.venv/

# Node / Next.js (apps/web)
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Cloudflare (apps/web)
.open-next/
.wrangler/
cloudflare-env.d.ts

# Build-time copy of the real /data (written by apps/web/scripts/build-data.mjs,
# imported statically by lib/raceData.ts/raceManifest.ts) -- not source, never
# committed. The real data lives at the repo root under /data. See
# docs/DECISIONS.md (2026-08-17).
/apps/web/data/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Editors / OS
.DS_Store
.vscode/

# Env files (secrets never committed)
.env
.env.local
.env*.local
